# Railway: set this service’s Root Directory to `backend` (folder containing this file).
# Do not use `cd backend` in Start Command when Root Directory is already `backend`.

[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
